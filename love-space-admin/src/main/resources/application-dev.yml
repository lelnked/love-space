spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/love_space
    username: love_space
    password: love_space
  jpa:
    properties:
      hibernate:
        format_sql: true
  liquibase:
    contexts: dev

app:
  jwt:
    secret: dev-only-jwt-secret-please-change-in-production-32+bytes!!!
  public-base-url: http://localhost:8080

logging:
  level:
    com.loves.space: DEBUG
