# 测试 / 联调环境：连接 docker 容器 lan_test_postgres（宿主机端口 25432）
spring:
  datasource:
    url: jdbc:postgresql://localhost:25432/love_space
    username: iris
    password: iris
  liquibase:
    contexts: dev

app:
  security:
    api-keys:
      - test-app-api-key-please-rotate

logging:
  level:
    com.space.app: DEBUG
