# love-space 开发文档

> ⚠️ 本文是**一期需求整理**，部分内容已过时（如运营账号已由 User 更名为 Manager、主键改 UUIDv7、异常改 GlobalExceptionHandler）。
> **实现约定以 `docs/{web,admin,app}-开发规范.md` 为准**，业务规则以 `openspec/specs/` 为准。

> 基于 `需求文档.pdf`（同类象 App v1.0 服务端 & 运营后台需求）与额外需求整理。
> 涉及三个子项目：
> - `love-space-web`：运营管理后台前端（React 19 + Vite + Tailwind v4）
> - `love-space-admin`：运营管理后台后端（Spring Boot 4.0.6 / Java 25）
> - `love-space-app`：移动端 App 后端（Spring Boot 4.0.6 / Java 25）

---

## 一、业务范围概览

模块名称：**爱女地图**

核心实体：
- **City 城市**：城市中英文名 / 省份中英文名 / 背景图 / 排序 / 上下线
- **Category 分类**：MVP 预留，运营可维护
- **Tag 爱女标签**：每个商户多标签；标签可上下架
- **Merchant 商户**：归属城市与分类，关联多个标签、多张图片、多条用户评价；带四维评分与权重
- **Period 推荐周期**（枚举）：月经期 / 卵泡期 / 排卵期 / 黄体期（多选）
- **User 运营用户**（额外需求）：角色 Admin / Member

四维评分（满分 100）：
| 维度 | 全名 | 满分 |
|---|---|---|
| S | `safetyEnvironmentScore`     女性安全环境 | 30 |
| L | `businessRightsScore`        女性经营权利 | 25 |
| E | `experienceFriendlyScore`    女性体验友好 | 25 |
| I | `socialContributionScore`    社会贡献价值 | 20 |

**百分制换算**：`单项百分制 = 运营填分 ÷ 当前维度满分 × 100`，只保留整数。
**爱女指数**：`总分 = S + L + E + I`，客户端展示分 10 级，每级对应半颗星。

---

## 二、love-space-app（移动端 App 后端 API）

> 客户端只读；MVP 无账号系统。所有路径前缀 `/api/app/**`。

### 2.1 接口列表

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/app/explore` | GET | 探索 tab 顶部聚合：当前城市信息 + 推荐 banner 列表（按运营排序，0/1/N 张）+ 空状态 |
| `/api/app/cities`  | GET | 已上线城市列表（按运营排序），用于城市切换 |
| `/api/app/merchants` | GET | 商户列表，参数：`cityId`(必填) / `period` / `categoryId`(预留) / `page` / `size`；排序 `weight DESC, createdAt DESC` |
| `/api/app/merchants/{id}` | GET | 商户详情，含图片、标签（仅已上架）、四维评分（百分制）、爱女指数（10 级）、用户评价、商户故事 |

### 2.2 返回字段要点
- 列表项：id、名称、logo、地址、已上架标签、四维百分制评分、爱女指数（10 级）
- 详情：名称、图片列表、logo、地址、坐标（预留可空）、已上架标签、四维百分制评分、爱女指数、用户评价（昵称/标题/正文，支持 emoji）、商户故事（≤5000 字）

### 2.3 计算规则（后端实现）
- 百分制换算与 10 级映射在 `merchant` 模块内的 `ScoreCalculator` 完成
- 标签筛选时仅返回上架状态；标签下架不影响商户本身状态

---

## 三、love-space-admin（运营后台 API）

> 路径前缀 `/api/admin/**`。所有接口需登录态；**用户管理接口要求 `ROLE_ADMIN`**。

### 3.1 认证
- `POST /api/admin/auth/login` — `username/password` → token + 当前用户信息
- `POST /api/admin/auth/logout`
- `GET  /api/admin/auth/me`

### 3.2 用户管理（仅 Admin，额外需求）
- `GET    /api/admin/users` — 分页 + 过滤：username 模糊 / role / 启用状态 / 创建时间
- `POST   /api/admin/users` — 新建用户，**强制 role=Member**（忽略入参 role）
- `GET    /api/admin/users/{id}`
- `PUT    /api/admin/users/{id}/enable`
- `PUT    /api/admin/users/{id}/disable`
- `PUT    /api/admin/users/{id}/password` — 重置密码（可选）

字段：`username`（唯一）/ `password`（BCrypt）/ `nickname` / `role`（ADMIN / MEMBER）/ `enable` / `createdAt`

初始化：启动时通过 `AdminUserInitializer` 注入默认 admin 账号
- 用户名：`admin`
- 密码：`8@y2eoRLyStM*UVU`
- 角色：ADMIN
- 启用：true

### 3.3 城市管理
- `GET    /api/admin/cities` — 分页 + 过滤（名称、上下线）
- `POST   /api/admin/cities` — 名称不重复
- `PUT    /api/admin/cities/{id}`
- `DELETE /api/admin/cities/{id}`
- `PUT    /api/admin/cities/{id}/online`、`/offline`
- `PUT    /api/admin/cities/{id}/sort`

字段：`chineseName` / `englishName` / `chineseProvince` / `englishProvince` / `backgroundImage` / `sortOrder` / `online`

### 3.4 分类管理（MVP 预留）
- `GET    /api/admin/categories`
- `POST   /api/admin/categories` — 名称不重复，≤10 汉字
- `PUT    /api/admin/categories/{id}`
- `DELETE /api/admin/categories/{id}` — 删除后该分类下商户自动下线
- `PUT    /api/admin/categories/{id}/sort`

### 3.5 爱女标签管理
- `GET    /api/admin/tags` — 分页 + 过滤（名称、上下架）
- `POST   /api/admin/tags` — 不重名，≤6 汉字
- `PUT    /api/admin/tags/{id}`
- `PUT    /api/admin/tags/{id}/online`、`/offline`

### 3.6 商户管理
- `GET    /api/admin/merchants` — 分页 + 过滤（名称 / cityId / categoryId / period / 上下架）
- `POST   /api/admin/merchants`
- `GET    /api/admin/merchants/{id}`
- `PUT    /api/admin/merchants/{id}`
- `DELETE /api/admin/merchants/{id}`
- `PUT    /api/admin/merchants/{id}/online`、`/offline`

字段：
- `name`（≤15 汉字，不校验唯一）
- `logo`（1 张）/ `images`（≥1 张）
- `recommendedPeriods`（多选 4 选 N）
- `categoryId`（单选）/ `cityId`（单选）
- `address`（纯文本）
- `longitude` / `latitude`（预留可空）
- `tags`（多选，无上限，按时间排序）
- 四维评分：`safetyEnvironmentScore` ≤30 / `businessRightsScore` ≤25 / `experienceFriendlyScore` ≤25 / `socialContributionScore` ≤20
- `reviews`：多条 `{nickname, title, content}`，支持 emoji（客户端固定五星展示，不取后台数据）
- `story`（≤5000 字纯文本）
- `online`（上下架）
- `weight`（权重，影响排序）

### 3.7 文件上传
- `POST /api/admin/files/upload` — 单图 ≤20MB，返回 URL

### 3.8 操作日志
- `GET /api/admin/logs` — 分页 + 过滤（操作人 / 模块 / 时间区间）

---

## 四、love-space-web（运营后台前端页面）

> 路由替换 TailAdmin 演示页。
> 所有列表页规范：
> 1. 顶部筛选区带 **Apply / Reset** 两个按钮
> 2. 分页器按 `support-tickets` 组件样式，**放在表格右下角**
> 3. 默认分页大小 20（可切换 20 / 30）

| 路由 | 页面 | 备注 |
|---|---|---|
| `/signin` | 登录 | 已有，需接 admin 登录 API |
| `/users` | 用户管理列表 | **仅 Admin 可见**，菜单按角色过滤 |
| `/users/create` | 新增用户 | UI 锁定 role=Member，不暴露选择 |
| `/cities` | 城市列表 | 含上下线 / 排序 |
| `/cities/create`、`/cities/:id/edit` | 城市表单 | |
| `/categories` | 分类列表 | MVP 预留 CRUD |
| `/tags` | 标签列表 | 含上下架 |
| `/merchants` | 商户列表 | 过滤：名称 / 城市 / 分类 / 周期 / 上下架 |
| `/merchants/create`、`/merchants/:id/edit` | 商户表单 | 分区：基础信息 / 图片(多图+logo) / 周期+分类+城市 / 地址坐标 / 标签 / 四维评分 / 用户评价（动态列表，支持 emoji）/ 故事 / 权重 / 上下架 |
| `/logs` | 操作日志 | 列：时间 / 操作人 / 角色 / 模块 / 动作 / 对象 / 结果 |

公共组件：
- 顶部用户菜单（当前用户 / 退出）
- 通用筛选区（Apply / Reset）
- 通用分页器（复用 `support-tickets` 样式）

---

## 五、数据模型概览（PostgreSQL）

> 所有字段使用全名（snake_case），不使用缩写。

- **User**(id, username[uk], password[bcrypt], nickname, role[ADMIN/MEMBER], enable, created_at)
- **City**(id, chinese_name, english_name, chinese_province, english_province, background_image, sort_order, online, created_at)
- **Category**(id, name, sort_order, created_at)
- **Tag**(id, name, online, created_at)
- **Merchant**(id, name, logo, address, longitude, latitude, city_id, category_id,
  safety_environment_score, business_rights_score, experience_friendly_score, social_contribution_score,
  story, weight, online, created_at)
- **MerchantImage**(id, merchant_id, url, sort_order)
- **MerchantPeriod**(merchant_id, period) — 多对多枚举
- **MerchantTag**(merchant_id, tag_id) — 多对多
- **MerchantReview**(id, merchant_id, nickname, title, content, sort_order)
- **OperationLog**(id, user_id, username, module, action, target, payload, created_at)

通用：所有实体继承 `BaseAuditEntity`（`createdAt / updatedAt / createdBy / updatedBy`），用 JPA Auditing 自动填充。

---

## 六、包结构

### 6.1 love-space-admin（根包：`com.loves.space`）

```
com.loves.space
├── LoveSpaceAdminApplication.java
│
├── common/                                  # 全局公共：与业务无关
│   ├── annotation/                          #   自定义注解（@OperationLog 等）
│   ├── constant/                            #   常量
│   ├── enums/                               #   通用枚举（Role, EnableStatus, Period…）
│   ├── exception/                           #   ValidationException / ResourceNotFoundException
│   ├── page/                                #   PageQuery 基类、Sort 工具
│   ├── util/                                #   工具类
│   └── validation/                          #   自定义校验注解
│
├── config/                                  # Spring 配置
│   ├── WebMvcConfig.java
│   ├── JpaConfig.java
│   ├── SecurityConfig.java
│   ├── SwaggerConfig.java
│   ├── AsyncConfig.java
│   └── properties/
│       └── JwtProperties.java
│
├── security/                                # 鉴权基础设施
│   ├── jwt/                                 #   JwtTokenProvider, JwtAuthFilter
│   ├── userdetails/                         #   AdminUserDetails, UserDetailsServiceImpl
│   ├── handler/                             #   AuthenticationEntryPoint, AccessDeniedHandler
│   └── OperatingContext.java                #   获取当前登录用户 / 操作上下文工具
│
├── infrastructure/                          # 基础设施
│   ├── storage/                             #   FileStorage + 本地/OSS 实现
│   └── log/                                 #   OperationLogAspect
│
├── web/                                     # 全局 Web 切面
│   └── ApiExceptionHandler.java             #   可选：仅美化字段级校验错误
│
└── modules/                                 # 业务模块（package-by-feature）
    ├── auth/      {controller, service, dto}
    ├── user/      {controller, service, repository, entity, dto, mapper, init/AdminUserInitializer}
    ├── city/      {controller, service, repository, entity, dto}
    ├── category/  {...}
    ├── tag/       {...}
    ├── merchant/  {controller, service, repository, entity, dto}
    ├── file/      {controller, dto}
    └── operationlog/ {controller, service, repository, entity, dto}
```

资源目录：

```
src/main/resources
├── application.yml
├── application-dev.yml
├── application-prod.yml
└── db/migration/
    ├── V1__init_schema.sql
    └── V2__seed_admin_user.sql
```

测试目录镜像主代码结构。

### 6.2 love-space-app（根包：`com.space.app`）

```
com.space.app
├── LoveSpaceAppApplication.java
│
├── common/                                  # 按需精简
│   ├── enums/
│   ├── exception/
│   ├── page/
│   └── util/
│
├── config/
│   ├── WebMvcConfig.java
│   ├── JpaConfig.java
│   └── SecurityConfig.java                  # MVP 无账号，permitAll + CORS
│
├── web/
│   └── ApiExceptionHandler.java             # 可选
│
└── modules/
    ├── explore/   {controller, service, dto}
    ├── city/      {controller, service, repository, entity, dto}
    ├── merchant/  {controller, service(ScoreCalculator), repository, entity, dto}
    └── tag/       {repository, entity}     # 被 merchant 依赖
```

### 6.3 命名 / 风格约定

- **包名**：`modules.<feature>.{controller,service,repository,entity,dto,mapper}`；不跨模块 import repository，跨模块走 service。
- **DTO**：入参 `XxxRequest` / `XxxQuery`，出参 `XxxResponse` / `XxxItem`。
- **Entity**：与表 1:1，不带 `Entity` 后缀。
- **字段全名**：实体属性、DTO 字段、数据库列一律使用完整英文单词，**禁止缩写**。
  - ❌ `scoreS / addr / lng / lat / bgImage / nameZh`
  - ✅ `safetyEnvironmentScore / address / longitude / latitude / backgroundImage / chineseName`
- **接口前缀**：admin → `/api/admin/**`，app → `/api/app/**`。
- **错误码**：按模块段位（1xxx auth, 2xxx user, 3xxx merchant…）。
- **日志**：admin 用 `@OperationLog("module:action")` AOP 切入；app 不写业务日志。
- **当前用户上下文**：统一通过 `OperatingContext` 获取，**不要使用 `CurrentUserHolder` 等命名**。

### 6.4 是否拆 common 模块

短期不拆。两个项目体量小，复制 `common/enums`、`exception` 等代码量 < 500 行比维护父 POM 更轻。
若后续共享实体超过 5 个再拆为：

```
love-space/
├── love-space-core/   # 共享 entity / enum / dto / util
├── love-space-admin/
└── love-space-app/
```

---

## 七、异常 / 校验设计

> 原则：**成功响应不包装**，Controller 直接返回 DTO / `Page<T>` / `void`；
> 异常返回沿用 **Spring Boot 默认 `ProblemDetail`（RFC 7807）**，不自造 `ApiResponse`。

### 7.1 业务异常

继承 Spring 内置的 `ErrorResponseException`，自动被默认处理器转为 `ProblemDetail`：

```java
public class ValidationException extends ErrorResponseException {
    public ValidationException(String detail) {
        super(HttpStatus.BAD_REQUEST,
              ProblemDetail.forStatusAndDetail(HttpStatus.BAD_REQUEST, detail),
              null);
    }
}

public class ResourceNotFoundException extends ErrorResponseException {
    public ResourceNotFoundException(String detail) {
        super(HttpStatus.NOT_FOUND,
              ProblemDetail.forStatusAndDetail(HttpStatus.NOT_FOUND, detail),
              null);
    }
}
```

Service 层直接 `throw new ValidationException("username 已存在")` → 自动 400。

### 7.2 Bean Validation

开启默认 ProblemDetail：

```yaml
spring:
  mvc:
    problemdetails:
      enabled: true
```

`@Valid` 失败 → Spring 默认 400 + ProblemDetail，无需自定义。

### 7.3 可选：字段级校验错误美化

仅在需要列出每个字段的错误时，加一个轻量 advice 覆盖默认行为：

```java
@RestControllerAdvice
public class ApiExceptionHandler extends ResponseEntityExceptionHandler {
    @Override
    protected ResponseEntity<Object> handleMethodArgumentNotValid(
            MethodArgumentNotValidException ex, HttpHeaders h, HttpStatusCode s, WebRequest r) {
        ProblemDetail pd = ProblemDetail.forStatus(HttpStatus.BAD_REQUEST);
        pd.setTitle("Validation Failed");
        pd.setProperty("errors", ex.getBindingResult().getFieldErrors().stream()
                .map(fe -> Map.of("field", fe.getField(), "message", fe.getDefaultMessage()))
                .toList());
        return ResponseEntity.badRequest().body(pd);
    }
}
```

其余异常（404、405、400 反序列化、ConstraintViolation 等）**全部交给 Spring 默认行为**。

### 7.4 响应示例

```jsonc
// 成功 200 — 直接返回业务对象
{ "id": 1, "username": "alice", "role": "MEMBER" }

// 校验失败 400 — Spring 默认 ProblemDetail
{
  "type": "about:blank",
  "title": "Validation Failed",
  "status": 400,
  "detail": "username 已存在",
  "instance": "/api/admin/users"
}
```

---

## 八、关键业务规则

1. **百分制 / 爱女指数** 在后端计算后返回，前端只渲染。
2. **用户管理**：接口 + 列表入口对非 Admin 完全隐藏（后端 `@PreAuthorize("hasRole('ADMIN')")` + 前端菜单按角色过滤）。新增用户接口强制 role=Member。
3. **鉴权**：Spring Security + JWT，密码 BCrypt。
4. **图片上传**：先本地 / MinIO，后续可接 OSS；统一返回 URL；单图 ≤20MB。
5. **删除分类** → 关联商户自动下线（service 层串联）。
6. **标签下架** → 商户详情/列表不返回该标签，但商户本身不下线。
7. **商户排序**：`weight DESC, createdAt DESC`。
8. **分页大小**：默认 20，前端可切换 20 / 30。
9. **操作日志**：admin 端关键写操作通过 `@OperationLog` 异步落库。

---

## 九、实现顺序建议

1. `love-space-admin` 基础设施：Security + JWT + User 实体 + `AdminUserInitializer` 初始化 admin
2. `love-space-web` 登录页 + 用户管理页（同时抽出通用 **筛选(Apply/Reset)** 与 **分页(support-tickets 风格)** 组件）
3. admin 端依次实现：城市 → 标签 → 分类 → 商户（含文件上传、操作日志）
4. 前端补齐对应 CRUD 页面
5. `love-space-app` 三个客户端接口（explore / merchants 列表 / merchants 详情 / cities）

---

## 十、待确认 / 后续讨论

- 性能要求：按需扩容方案待具体讨论
- 图片存储：本地 vs MinIO vs 公有云 OSS 的最终选型
- 客户端坐标字段：本期是否实际写入，还是仅 schema 预留
- 分类筛选：是否本期上线（运营评估）
